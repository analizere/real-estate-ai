---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - app/account/settings/page.tsx
  - app/account/layout.tsx
  - components/account/subscription-status-card.tsx
  - components/account/cancel-subscription-dialog.tsx
  - app/page.tsx
autonomous: false
requirements:
  - BILL-03
  - BILL-04
  - BILL-06
  - API-01

must_haves:
  truths:
    - "Authenticated user can see their subscription status (free/pro/cancelled) on the account settings page"
    - "Pro user can cancel their subscription via a confirmation dialog"
    - "Free user sees an Upgrade to Pro button with contextual description"
    - "Cancelled user sees access-until date and visual indicator"
    - "Account settings page uses sidebar layout on desktop, stacked on mobile"
  artifacts:
    - path: "app/account/settings/page.tsx"
      provides: "Account settings page with subscription management"
      contains: "Account Settings"
    - path: "app/account/layout.tsx"
      provides: "Account section layout with sidebar nav"
      contains: "SidebarLayout"
    - path: "components/account/subscription-status-card.tsx"
      provides: "Subscription status display with upgrade/cancel actions"
      contains: "Upgrade to Pro"
    - path: "components/account/cancel-subscription-dialog.tsx"
      provides: "Cancel confirmation dialog"
      contains: "Cancel your Pro subscription"
  key_links:
    - from: "components/account/subscription-status-card.tsx"
      to: "hooks/use-subscription.ts"
      via: "useSubscription() hook for tier status"
      pattern: "useSubscription"
    - from: "components/account/subscription-status-card.tsx"
      to: "lib/auth-client.ts"
      via: "authClient.subscription.upgrade() for Stripe redirect"
      pattern: "subscription\\.upgrade"
    - from: "components/account/cancel-subscription-dialog.tsx"
      to: "app/api/v1/billing/portal/route.ts"
      via: "fetch to billing cancel endpoint"
      pattern: "api/v1/billing"
---

<objective>
Build the account settings page with subscription status display, upgrade flow, and cancel subscription dialog. Also update the home page to show navigation and a basic authenticated/unauthenticated state. This completes all user-facing billing UI for Phase 1.

Purpose: Users can see their subscription tier, upgrade to Pro via Stripe Checkout, and cancel their subscription — closing out all BILL requirements for Phase 1.
Output: Account settings page with subscription management, updated home page with navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-CONTEXT.md
@hooks/use-auth.ts
@hooks/use-subscription.ts
@lib/auth-client.ts

<interfaces>
From hooks/use-auth.ts (Plan 01):
```typescript
export function useAuth(): {
  user: { id: string; name: string; email: string; image?: string } | null;
  isAuthenticated: boolean;
  isLoading: boolean;
}
```

From hooks/use-subscription.ts (Plan 01):
```typescript
export function useSubscription(): {
  status: "free" | "active" | "trialing" | "cancelled" | "loading";
  isPro: boolean;
  isFree: boolean;
  isCancelled: boolean;
  isLoading: boolean;
  periodEnd: Date | null;
}
```

From lib/auth-client.ts (Plan 01):
```typescript
export const authClient; // .subscription.upgrade({ plan, successUrl, cancelUrl })
export const signOut; // signOut()
```

From components/ui/ (Plan 01 + 02):
- Card, Badge, Button, Dialog, Separator, Spinner, Avatar
- SidebarLayout, TopNav, BottomTabBar, UserMenuDropdown
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build account settings page with subscription management</name>
  <files>
    app/account/layout.tsx
    app/account/settings/page.tsx
    components/account/subscription-status-card.tsx
    components/account/cancel-subscription-dialog.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md
    hooks/use-auth.ts
    hooks/use-subscription.ts
    lib/auth-client.ts
    components/ui/card.tsx
    components/ui/badge.tsx
    components/ui/button.tsx
    components/ui/dialog.tsx
    components/ui/sidebar-layout.tsx
    components/ui/avatar.tsx
  </read_first>
  <action>
1. **`app/account/layout.tsx`** — Account section layout (per UI-SPEC Layout Contract for Account Settings):
   `"use client"` component (uses hooks for auth state).
   Uses SidebarLayout component. Sidebar contains navigation links:
   - "Settings" (active by default, href `/account/settings`)
   - Future links placeholder (disabled/greyed): "Deal History" (Phase 2), "Usage" (Phase 5)

   Desktop: 240px sidebar + flex-1 content area (per UI-SPEC: `max-w-[640px]` content).
   Mobile: sidebar collapses to a horizontal tab row at top.

   Also wraps with TopNav on desktop and BottomTabBar on mobile:
   - TopNav: logo text "Real Estate AI", nav items (just "Home" href `/` for Phase 1), UserMenuDropdown on right
   - BottomTabBar: items for Home (icon: Home), Settings (icon: Settings)

2. **`components/account/subscription-status-card.tsx`** — Subscription status display (per UI-SPEC focal point for account settings):
   `"use client"` component.
   Props: none (uses hooks internally — `useSubscription()` and `useAuth()`).

   Three states based on subscription status:

   **Free tier:**
   - Card with heading "Your Plan"
   - Badge variant default: "Free"
   - Description: "Free plan — unlimited manual analyses" (per Copywriting Contract)
   - Upgrade CTA: Button `variant="default"` (accent color) with text "Upgrade to Pro"
   - Subtext below button: "Unlock automated public records, DADU feasibility, and rent estimates." (per Copywriting)
   - On click: `authClient.subscription.upgrade({ plan: "pro", successUrl: window.location.origin + "/account/settings?upgraded=true", cancelUrl: window.location.origin + "/account/settings" })`

   **Pro tier (active):**
   - Badge variant success: "Pro"
   - Description: "Pro plan — automated data pulls active" (per Copywriting)
   - Show period end date if available: "Renews on {date}"
   - Cancel button: Button `variant="ghost"` with `text-destructive` color, text "Cancel subscription" (per Copywriting)
   - On cancel click: open CancelSubscriptionDialog

   **Cancelled:**
   - Badge variant warning: "Pro (Cancelled)"
   - Description: "Pro plan cancelled — access continues until {date}" (per Copywriting, with actual period end date)
   - Resubscribe button: "Upgrade to Pro" (same as free tier)

   **Loading state:** Skeleton loader while useSubscription returns `isLoading: true`

3. **`components/account/cancel-subscription-dialog.tsx`** — Cancel confirmation dialog (per UI-SPEC):
   `"use client"` component.
   Props: `open` (boolean), `onOpenChange` (callback), `periodEnd` (Date | null).

   Uses Dialog component from shadcn. Content:
   - Heading: "Cancel your Pro subscription?" (per Copywriting)
   - Body: "You'll keep Pro access until the end of your billing period on {date}. After that, your account reverts to the free plan." (per Copywriting, with formatted date)
   - Two buttons:
     - "Keep my subscription" — Button variant ghost, calls `onOpenChange(false)` (per Copywriting)
     - "Yes, cancel subscription" — Button variant destructive (per Copywriting)
   - On confirm: POST to `/api/v1/billing/portal` to get the Stripe Customer Portal URL, then `window.location.href = portalUrl`. The portal lets users manage their subscription directly with Stripe (Stripe handles the cancel flow). This is simpler and more reliable than building custom cancel logic.

   Loading state: Show Spinner in the confirm button while processing.

4. **`app/account/settings/page.tsx`** — Account settings page:
   `"use client"` component.

   Layout (within the account layout's content area, max-width 640px per UI-SPEC):
   - Page heading: "Account Settings"
   - SubscriptionStatusCard at top (focal point per UI-SPEC)
   - Separator
   - Profile section: Display user name, email, and avatar (from useAuth hook)
   - Show "Signed in with Google" badge if applicable
   - Future sections placeholder (greyed labels): "Notification preferences" (future), "Delete account" (future)

   Check URL params: if `?upgraded=true`, show a success toast "Welcome to Pro! Automated data pulls are now active."
  </action>
  <verify>
    <automated>test -f app/account/settings/page.tsx && test -f app/account/layout.tsx && test -f components/account/subscription-status-card.tsx && test -f components/account/cancel-subscription-dialog.tsx && npx tsc --noEmit 2>&1 | head -20 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `app/account/layout.tsx` contains `"use client"` and uses `SidebarLayout` component
    - `app/account/layout.tsx` contains `TopNav` and `BottomTabBar` components
    - `components/account/subscription-status-card.tsx` contains `useSubscription` hook import
    - `components/account/subscription-status-card.tsx` contains text "Upgrade to Pro" and "Cancel subscription"
    - `components/account/subscription-status-card.tsx` contains `authClient.subscription.upgrade` call
    - `components/account/subscription-status-card.tsx` contains all three tier descriptions: "Free plan — unlimited manual analyses", "Pro plan — automated data pulls active", "Pro plan cancelled — access continues until"
    - `components/account/cancel-subscription-dialog.tsx` uses Dialog component and contains "Cancel your Pro subscription?" heading
    - `components/account/cancel-subscription-dialog.tsx` contains "Keep my subscription" and "Yes, cancel subscription" button texts
    - `app/account/settings/page.tsx` renders `SubscriptionStatusCard` component
    - `app/account/settings/page.tsx` contains check for `upgraded=true` URL param
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Account settings page built with three-state subscription status card (free/pro/cancelled), upgrade button wired to Stripe Checkout, cancel dialog with confirmation copy, and account layout with sidebar navigation on desktop / tab bar on mobile.</done>
</task>

<task type="auto">
  <name>Task 2: Update home page with navigation and auth-aware state</name>
  <files>
    app/page.tsx
  </files>
  <read_first>
    app/page.tsx
    hooks/use-auth.ts
    components/ui/top-nav.tsx
    components/ui/bottom-tab-bar.tsx
    components/ui/button.tsx
  </read_first>
  <action>
Replace the existing prototype home page (`app/page.tsx`) with a minimal authenticated landing page. This is temporary — Phase 2 builds the real calculator UI.

`"use client"` component.

Structure:
- TopNav with logo "Real Estate AI", nav items (just "Home" active for Phase 1)
  - If authenticated: UserMenuDropdown with user data from useAuth()
  - If not authenticated: "Sign in" Button (link variant) pointing to `/sign-in`
- BottomTabBar on mobile with Home tab
- Main content area (PageWrapper):
  - If authenticated: "Welcome back, {user.name}" heading, "Your dashboard is coming in Phase 2" placeholder text, link to Account Settings
  - If not authenticated: "Analyze any deal in 60 seconds." headline (Display: 28px/600), description text, two CTAs: "Create account" (primary, links to `/sign-up`) and "Sign in" (secondary, links to `/sign-in`)

Remove all prototype property listing code and mock-properties imports.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && grep -q "useAuth\|use-auth" app/page.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `app/page.tsx` contains `"use client"` directive
    - `app/page.tsx` imports `useAuth` from hooks
    - `app/page.tsx` contains `TopNav` component
    - `app/page.tsx` contains `BottomTabBar` component
    - `app/page.tsx` does NOT import from `lib/mock-properties`
    - `app/page.tsx` contains "Analyze any deal in 60 seconds" text for unauthenticated state
    - `app/page.tsx` contains link to `/sign-up` and `/sign-in`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Home page updated from prototype to auth-aware landing: shows welcome for authenticated users, marketing CTA for unauthenticated users, with TopNav/BottomTabBar navigation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify auth and billing flow end-to-end</name>
  <files>none — verification only</files>
  <action>
Human verifies the complete Phase 1 auth and billing flow end-to-end. All env vars must be configured in `.env.local` before testing. Database must be migrated. Stripe CLI must be running for webhook forwarding.
  </action>
  <what-built>Complete auth system (sign-up, sign-in, Google OAuth, email verification, password reset) and billing system (subscription status, Stripe checkout, cancel flow) with all UI pages and API endpoints.</what-built>
  <how-to-verify>
    Prerequisites: All env vars must be configured in `.env.local` (copy from `.env.example` and fill in values from Neon, Stripe, Resend, Google Cloud). Run `npx drizzle-kit migrate` to create DB tables. Run `stripe listen --forward-to localhost:3000/api/auth/stripe/webhook` in a separate terminal.

    1. Start dev server: `npm run dev`
    2. Visit http://localhost:3000 — verify unauthenticated landing page shows "Analyze any deal in 60 seconds" headline with Sign in / Create account buttons
    3. Click "Create account" — verify sign-up form appears centered with Display headline, email/password/name fields, Google OAuth button
    4. Fill in test credentials and submit — verify redirect to /verify-email with "Check your inbox" message
    5. Check Resend dashboard (or email) for verification email — click the link
    6. Sign in with the verified credentials — verify redirect to home page with "Welcome back" message
    7. Navigate to Account Settings — verify subscription shows "Free plan" with "Upgrade to Pro" button
    8. Click "Upgrade to Pro" — verify redirect to Stripe Checkout (test mode)
    9. Complete checkout with Stripe test card (4242 4242 4242 4242) — verify redirect back with "Welcome to Pro" toast
    10. Verify subscription status shows "Pro plan — automated data pulls active"
    11. Click "Cancel subscription" — verify dialog appears with correct copy
    12. Test "Forgot your password?" flow from sign-in page
    13. Test dark mode toggle (if system preference changes)
    14. Test on mobile viewport (375px) — verify bottom tab bar, no horizontal scroll, all touch targets accessible
  </how-to-verify>
  <verify>Human confirms all 14 test steps pass</verify>
  <done>All auth flows (sign-up, sign-in, Google OAuth, email verification, password reset) and billing flows (subscription status, upgrade, cancel) work end-to-end on localhost with real external services.</done>
  <resume-signal>Type "approved" if all flows work, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Account settings page exists with subscription status card
3. Cancel dialog exists with correct copy
4. Home page is auth-aware (different states for authenticated/unauthenticated)
5. All UI components from Plan 02 are consumed correctly
6. Human verification confirms end-to-end auth + billing flow works
</verification>

<success_criteria>
- Account settings page shows subscription status (free/pro/cancelled) with correct copy
- Upgrade to Pro button triggers Stripe Checkout redirect
- Cancel subscription dialog shows confirmation with period end date
- Home page shows auth-aware state (welcome vs. marketing CTA)
- TopNav with UserMenuDropdown on desktop, BottomTabBar on mobile
- Human verifies: sign-up, verification email, sign-in, subscription upgrade, cancel flow all work end-to-end
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
